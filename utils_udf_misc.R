wordswitch <- function(str) {
  switch(str,
         conf = "confidence level",
         pow = "power",
         conv = "test - conversion rate",
         size = "sample size",
         n2 = " of control",
         n1 = " of test")
}